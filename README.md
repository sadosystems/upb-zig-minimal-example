# μpb-Zig Minimal Example   
This is a minimal example of the use of upb-zig in a regular Zig project.

If you haven't installed the `protoc` compiler, follow the instructions in [Protocol Buffer Compiler Installation](https://protobuf.dev/installation/).
If you are new to `Zig`, follow the instructions in [Zig Getting Started](https://ziglang.org/learn/getting-started/).

Run
```shell
zig zig build run
```
